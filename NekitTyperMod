from .. import loader, utils 

@loader.tds
class NekitMod(loader.Module):
    """Модуль со стилями тайпинга"""
    strings = {'name': 'nekitTyperModule'}
               
    async def client_ready(self, client, db):
        self.db = db
        self.client = client
        
    async def ticker(self, message):
        """Текст с бегущей строкой""" 
